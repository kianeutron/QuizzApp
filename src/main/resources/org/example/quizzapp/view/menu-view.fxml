<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>

<VBox alignment="CENTER" spacing="30.0" prefWidth="800.0" prefHeight="600.0"
      xmlns="http://javafx.com/javafx/11.0.1" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.quizzapp.controller.MenuController">
   <children>
      <!-- Title Section -->
      <VBox alignment="CENTER" spacing="10.0">
         <children>
            <Label fx:id="titleLabel" styleClass="title-label" text="🎯 QuizMaster" />
            <Label styleClass="description-label" text="Test Your Knowledge!"
                   style="-fx-text-fill: white; -fx-font-size: 22px;" />
         </children>
      </VBox>

      <!-- Spacer -->
      <Region VBox.vgrow="SOMETIMES" minHeight="20.0" />

      <!-- Quiz Info Card -->
      <VBox alignment="CENTER" spacing="15.0"
            style="-fx-background-color: rgba(255,255,255,0.95); -fx-background-radius: 20px; -fx-padding: 30px; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 15, 0, 0, 5);">
         <children>
            <Label fx:id="quizInfoLabel" styleClass="section-label" text="No quiz loaded"
                   style="-fx-text-fill: #667eea;" />
         </children>
      </VBox>

      <!-- Spacer -->
      <Region VBox.vgrow="SOMETIMES" minHeight="20.0" />

      <!-- Buttons Section -->
      <VBox alignment="CENTER" spacing="20.0">
         <children>
            <Button fx:id="loadQuizButton" mnemonicParsing="false" onAction="#handleLoadQuiz"
                    styleClass="primary-button" text="📂 Load Quiz" prefWidth="300.0" />
            <Button fx:id="startQuizButton" mnemonicParsing="false" onAction="#handleStartQuiz"
                    styleClass="success-button" text="▶️ Start Quiz" prefWidth="300.0" />
            <Button fx:id="viewResultsButton" mnemonicParsing="false" onAction="#handleViewResults"
                    styleClass="secondary-button" text="📊 View Results" prefWidth="300.0" />
         </children>
      </VBox>
   </children>
   <padding>
      <Insets bottom="60.0" left="60.0" right="60.0" top="60.0" />
   </padding>
</VBox>
